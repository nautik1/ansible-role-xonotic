sv_public {{ make_public }}	// 0 or -1 (see description) if the server should not be on the public servers list
hostname {{ server_name }}	// this name will appear on the server list (the $g_xonoticversion gets replaced with the current version)
sv_motd {{ welcome_message }}	// displayed on connect - good place for contact info (IRC, forum nick, etc.) so players can reach you when something is wrong with your server

// This sets the remote console password on the server, you also need to set the cvar to the same value on your client to use the 'rcon' command.
// Empty means rcon is disabled.
rcon_password {{ rcon_password }}

{{ extra_configuration }}
